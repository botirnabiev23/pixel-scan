import 'package:flutter/material.dart';

class DocumentViewerPage extends StatelessWidget {
  final String id;

  const DocumentViewerPage({super.key, required this.id});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
